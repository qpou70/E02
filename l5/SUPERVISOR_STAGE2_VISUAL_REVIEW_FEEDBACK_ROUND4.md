# ECE340 L5 第二阶段 ROUND3：第四轮视觉监工意见

## 1. 总结论

当前检查文件：

`l5/ECE340_L5_S18_Posted_中文忠实重建_第二阶段视觉返修ROUND3_第16_17_24页.pptx`

对应 generation commit：

`7306418bbf3d2a9d19053f660d3c896e8cf99dc9`

本轮监工已从 GitHub Actions artifact 只读取回 ROUND3 的 3 张高清渲染 PNG 与 3 张 `Original PDF vs New Page` 对照图，并逐页实际打开检查。

结论：**ROUND3 仍未完全通过，但目前只剩第 16 页需要最后一次定点返修。第 17、24 页通过并冻结。**

### 本轮通过并冻结

- 第 17 页：通过。
- 第 24 页：通过。

以及此前已经通过的第 8、9、10、11、12、13、14、15、18、19、20、21、22、23 页继续冻结。

### 本轮唯一不通过

- 第 16 页：科学图已经恢复，但仍存在关键标签缺失和残留英文图内说明，尚不满足中文忠实重建。

---

## 2. 第 17 页 —— 通过，冻结

实际高清渲染确认：

- 标题为 `原子轨道的电子占据`；
- 表格完整显示到 `36 Kr`；
- `33 As / 34 Se / 35 Br / 36 Kr` 均肉眼可见；
- 红箭头、红色虚线框、Si 黄色高亮均保留；
- 右上小标题已恢复为 `Population of Atomic Orbitals`；
- 没有再裁掉表格底部。

**不得再修改。**

---

## 3. 第 24 页 —— 通过，冻结

实际高清渲染确认：

- 右下标题已经改为 `N 个 Si 原子形成晶体时`；
- 原错误 `N ↑ Si 原子形成晶体时` 已消失；
- 两个 `Total` 已改为 `合计`；
- Si 电子组态、n=3 状态计数、导带/价带状态数和 0 K 占据关系均保持正确；
- 页面无明显重叠、出界或残片。

**不得再修改。**

---

## 4. 第 16 页 —— 仍不通过，只做定点修复

本轮比 ROUND2 已明显改善：

- 右侧整套英文 bullet list 已删除；
- 原 PDF 橙色标题条已删除；
- 三组科学图已经恢复；
- 左侧四类键合中文说明完整。

但仍存在两个问题。

### 问题 A：电负性差 `ΔE` 三幅图缺少原页的关键对应标签

原 PDF 在三幅电子云图上明确标注：

- `ΔE = 0 (covalent bond)`
- `ΔE (intermediate) (polar covalent bond)`
- `ΔE (large) (ionic)`

这三组标签是本页用来说明“电负性差如何对应共价 → 极性共价 → 离子”的核心教学关系。

当前 ROUND3 只有一个总标题：

`电负性差 ΔE 与电子云极化`

但三幅图之间没有明确写出各自对应的 `ΔE` 大小和键型，因此原页教学关系仍然不完整。

**必须补回三组中文标签：**

- 左图：`ΔE = 0（共价键）`
- 中图：`ΔE 中等（极性共价键）`
- 右图：`ΔE 较大（离子键）`

标签应直接位于对应三幅电子云图的上方或下方，清楚一一对应，不得遮挡图像。

### 问题 B：科学图内部仍残留两处可翻译的英文说明

当前高清渲染中仍可见：

1. 共价键晶格图下方：`Two electrons per bond`
2. 金属键图下方：`Swarm of delocalised electrons`

这些不是不可替代的标准符号，而是明确的英文教学说明。既然本页已经进入中文重建阶段，不能继续保留为可见英文说明。

**必须中文化：**

- `Two electrons per bond` → `每个键由两个电子共享`（或 `每个键含两个电子`）
- `Swarm of delocalised electrons` → `离域电子海`

处理方式：

- 可以在原英文文字所在的局部区域用与背景一致的颜色干净覆盖，再放置中文；
- 不得覆盖原科学图结构；
- 不得移动晶格、电子云或金属键图本体；
- 不得重新设计或重新绘制整张科学图。

### 第 16 页本轮禁止事项

- 不得修改左侧四个中文说明框；
- 不得再引入整块英文原页；
- 不得调用 image generation；
- 不得重画科学关系；
- 不得删除 `ΔE`；
- 不得把三幅极化图合并成模糊的一般性说明；
- 不得改动第 17、24 页或其他已通过页。

---

## 5. 下一轮严格范围

### 只允许修改

- 第 16 页

### 必须冻结

- 第 8–15 页
- 第 17–24 页
- 第 1–7 页
- 第 25–52 页

也就是说，**整个下一轮只准动第 16 页。**

---

## 6. 下一轮交付要求

完成后必须生成：

1. 第二阶段 ROUND4 PPTX；
2. 第 16 页高清 PNG；
3. 第 16 页 `Original PDF vs New Page` 对照图；
4. ROUND4 单页 contact sheet（可选，但如现有 workflow 统一生成则保留）；
5. ROUND4 build report；
6. generation commit SHA；
7. packaging / trigger commit SHA（如有）；
8. current branch HEAD。

提交前必须实际打开最终 `page_16.png` 肉眼确认：

- 三幅电子云图分别有：
  - `ΔE = 0（共价键）`
  - `ΔE 中等（极性共价键）`
  - `ΔE 较大（离子键）`
- `Two electrons per bond` 不再可见；
- `Swarm of delocalised electrons` 不再可见；
- 对应中文说明清楚且没有遮挡科学图；
- 无橙色残线；
- 无大段英文正文；
- 无文字重叠或出界。

完成后等待 supervisor visual acceptance，不得自行进入最终合并阶段。
