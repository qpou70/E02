# ECE340 L5 第一阶段返修：第三轮视觉监工意见

## 1. 结论

当前第一阶段版本**仍不通过最终视觉验收**。

本轮检查基于 worker 分支 `l5-stage1-strict-rework` 的最新提交与实际高清渲染图，而不是依据 worker 的自检文字。

当前 worker HEAD：`a8167a96c54745044d88a3acd1ae26e796502ad1`

本轮生成候选提交：`4db53fd057a96aa4eca9d3ebc38b825dd0b1a11d`

### 已通过并冻结

- 第 9 页：通过，继续冻结。
- 第 12 页：本轮通过，冻结。
- 第 18 页：通过，继续冻结。
- 第 20 页：通过，继续冻结。

### 仍需返修

- **第 23 页：仍不通过。**

因此第一阶段整体仍未通过，**禁止开始第二阶段**。

---

## 2. 第 12 页：本轮通过

本轮右侧显微图已经去掉上一版额外贴上的黑底 `<100>` 补丁，也清理了底部残片。当前渲染视觉上是一张完整、干净的原始显微图，没有再出现后贴标签、孤立碎字或重复标注。

右下区域按原始扫描图忠实保留为 `100` 与方向线，不再人为制造一个 `<100>` 黑框，这是正确处理。

### 第 12 页后续要求

- 冻结，不再修改；
- 不再为了“满足文字断言”往显微图上添加任何人工补丁；
- 后续合并总稿时保持该页当前 XML 和视觉结果不变。

---

## 3. 第 23 页：科学图示忠实度已经解决，但中文化仍明显不合格

本轮采用了上一轮反馈中的“方案 B”：直接保留原 PDF 的完整科学图示区域。这个方案确实解决了上一轮最严重的问题——波函数曲线、节点、成键/反键关系、电子密度、双势阱 `V(r)` 与能级分裂关系都不再被自制简图篡改。

但是从**最终学生课件的视觉稿**来看，这一页现在仍然不能验收。

### 当前最明显的问题

整页主体几乎又回到了原始英文课件：

- 左上仍是大段英文：`Elemental Hydrogen: 1s¹ / Hydrogen #1 / Hydrogen #2 / H₂ ...`；
- 图中仍保留 `Atomic orbitals / Antibonding orbital / Bonding orbital / Higher Energy / Lower Energy`；
- 仍有 `Electron not located between hydrogen atoms / Electron located between hydrogen atoms`；
- 能级标注仍为 `Antibonding energy level / Bonding energy level`；
- 页面底部仍保留整段英文 LCAO 说明文字；
- `Figure 3.2` 仍为英文图注。

虽然科学关系忠实了，但**中文忠实重建任务并不是把英文原页整块缩进去，再只换一个中文标题**。

用户此前已经明确指出“第 23 页仍保留大段英文”属于不合格项。因此上一轮给出的“整块保留原科学图示区域即可”的 fallback 对这一页过于宽松，本轮正式收紧：

> **第 23 页必须在保留原科学曲线/节点/势阱/能级关系不变的前提下，完成主要教学文字的中文化。**

---

## 4. 第 23 页正确返修方式：只改文字，不再重画科学图形

这一次**禁止重新设计科学图示**。不要再画新的波函数、圆形原子、简化能级线或新的 `V(r)` 图。

应以当前已经忠实保留的原图为底图，采用“保留图形 + 去英文文字 + 原位中文替换”的方式。

### 必须保留不动的图形元素

以下科学图形不得改变：

1. `ψ1`、`ψ2` 两个 1s 原子轨道波函数曲线；
2. 上方反键轨道波函数及节点、正负相位；
3. 下方成键轨道波函数及两核间增强；
4. 左下成键/反键电子密度示意；
5. 中央双势阱 `V(r)` 曲线；
6. 蓝色虚线/实线对应的成键与反键能级分裂关系；
7. 原始箭头、核位置、节点和图示空间关系。

### 必须中文化的可见文字

建议直接按以下对应替换，不要再用红框或悬浮贴纸：

- `Elemental Hydrogen: 1s¹` → `氢原子：1s¹`
- `Hydrogen #1: 2 states, 1 electron` → `氢原子 1：2 个状态，1 个电子`
- `Hydrogen #2: 2 states, 1 electron` → `氢原子 2：2 个状态，1 个电子`
- `H₂: 4 states, 2 electrons` → `H₂：4 个状态，2 个电子`
- `Atomic orbitals` → `原子轨道`
- `Antibonding orbital` → `反键轨道`
- `Bonding orbital` → `成键轨道`
- `Higher Energy` → `较高能量`
- `Lower Energy` → `较低能量`
- `Electron not located between hydrogen atoms` → `电子不位于两核之间`
- `Electron located between hydrogen atoms` → `电子位于两核之间`
- `Antibonding energy level` → `反键能级`
- `Bonding energy level` → `成键能级`
- `Figure 3.2` → `图 3.2`

符号 `ψ1`、`ψ2`、`V(r)`、`σ`、`σ*` 等科学符号保留原样，不要翻译或改写。

### 页面底部英文段落必须处理

当前底部整段英文不能继续作为学生主要阅读文字。

可以把原英文段落从可见页面中移除，并在原位置重建为中文说明，完整表达原文的三层逻辑：

1. 两个原子靠近时，两个原子轨道通过 LCAO 形成两个不同的“简正”模式：高能反键轨道与低能成键轨道；
2. 成键态的电子概率密度在离子实之间增大，使成键能降低并增强内聚；
3. 当不是 2 个原子而是 N 个原子靠近时，会形成 N 个不同的 LCAO 与 N 个彼此接近的能级，最终形成能带。

可采用如下中文文本，允许按版面微调字号和换行，但不得删掉上述三层信息：

> 原子轨道线性组合（LCAO）：两个原子靠近时，原子轨道组合形成两个不同的“简正”模式——较高能量的反键轨道和较低能量的成键轨道。成键态电子的概率密度在两个离子实之间增大，从而降低成键能并增强体系内聚。若由 2 个原子扩展到 N 个原子，则会形成 N 个不同的 LCAO 以及 N 个彼此接近的能级，最终形成能带。

如果页面空间不足，可把来源网址保留为底部很小的一行；但不能因为网址占空间而保留英文正文。

---

## 5. 第 23 页视觉方式要求

为了避免重新出现“红框贴纸”问题：

- 不使用红框；
- 不使用带边框的浮动翻译卡片；
- 不在英文原词旁边再叠一份中文形成重复；
- 应先把原英文文字区域用白底/背景色干净覆盖，再在同一逻辑位置直接放中文；
- 中文标签必须与原箭头、曲线、轨道和能级保持原位对应；
- 页面整体应看起来是一张完成版中文课件，而不是“英文截图 + 翻译补丁”。

可以保留少量必要英文缩写或科学符号，但**主要教学文字必须以中文为主**。

---

## 6. 下一轮只允许修改第 23 页

下一轮范围进一步收紧：

- 第 9 页：冻结；
- 第 12 页：冻结；
- 第 18 页：冻结；
- 第 20 页：冻结；
- **只允许修改第 23 页。**

禁止开始第二阶段。

完成后必须重新输出并提交：

1. 新的 `page_23.png`；
2. 新的 `page_23_original_pdf_vs_new.jpg`；
3. 更新后的 5 页 contact sheet；
4. 更新后的 build report；
5. 新 PPTX；
6. GitHub commit SHA。

并继续保持第 9、12、18、20 页与当前通过版一致。

---

## 7. 本轮最终判定

- 第 9 页：**通过，冻结**。
- 第 12 页：**通过，冻结**。
- 第 18 页：**通过，冻结**。
- 第 20 页：**通过，冻结**。
- 第 23 页：**不通过——科学图示关系已恢复，但主要教学文字仍为大段英文，未完成中文忠实重建。**

**第一阶段整体：仍不通过。下一轮只改第 23 页。**